import { GenerateCellDirective } from './generate-cell.directive';

describe('GenerateCellDirective', () => {
  it('should create an instance', () => {
    const directive = new GenerateCellDirective();
    expect(directive).toBeTruthy();
  });
});
